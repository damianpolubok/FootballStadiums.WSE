<UserControl x:Class="FootballStadiums.WSE.Views.StadiumFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="400">

    <Border Background="White" CornerRadius="10" Padding="30" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="0" Opacity="0.3" BlurRadius="20"/>
        </Border.Effect>

        <StackPanel>
            <Grid Margin="0,0,0,20">
                <TextBlock x:Name="HeaderTxt" Text="Edit Stadium" FontSize="24" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center"/>
                <Button Content="✕" Click="Cancel_Click" Background="Transparent" Foreground="#999" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="5"/>
            </Grid>

            <TextBlock Text="Stadium Name" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTxt" Margin="0,0,0,15"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="City" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <TextBox x:Name="CityTxt" Margin="0,0,0,15"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Country" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <TextBox x:Name="CountryTxt" Margin="0,0,0,15"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Street Address" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
            <TextBox x:Name="StreetTxt" Margin="0,0,0,15"/>

            <TextBlock Text="Image URL" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
            <TextBox x:Name="ImageTxt" Margin="0,0,0,15"/>

            <TextBlock Text="Clubs (Comma separated)" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
            <TextBox x:Name="ClubsTxt" Margin="0,0,0,25"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="Cancel_Click" Background="#EEE" Foreground="#333" Margin="0,0,10,0" Width="100"/>
                <Button Content="Save Changes" Click="Save_Click" Width="120"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>